# CONFIGS

This directory currently contains release-safe templates only. Original training settings are still being audited from shell scripts and SCOW/Linguistics job files.

Do not invent missing hyperparameters. Move a setting here only after it has been verified from the original experiment command, config, or log.

