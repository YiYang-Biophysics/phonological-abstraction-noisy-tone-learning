# Analysis

Planned public entry points:

- `build_result_tables.py`
- `plot_paper_figures.py`

These scripts should consume only release-safe CSVs under `../../data/` and write figures/tables without requiring private paths.

