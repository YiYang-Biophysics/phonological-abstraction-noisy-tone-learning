# Cluster Templates

This directory is reserved for public Slurm templates.

Do not include private SCOW scripts verbatim. Release templates should use placeholders such as:

```text
<PROJECT_ROOT>
<AISHELL_ROOT>
<CHECKPOINT_ROOT>
<FAIRSEQ_ROOT>
```

Original cluster entry points are documented in `../../docs/ORIGINAL_CODE_INDEX.md`.

