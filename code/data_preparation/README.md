# Data Preparation

Planned public entry points:

- `prepare_aishell_manifests.py`
- `generate_scale_subsets.py`
- `generate_label_noise.py`

AISHELL-1 raw audio is not redistributed in this repository. Scripts should accept `--aishell-root` and write release-safe manifests using relative paths or placeholders.

