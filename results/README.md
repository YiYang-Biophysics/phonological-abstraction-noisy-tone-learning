# RESULTS

The canonical released result CSVs currently live under `data/` because they are also direct dashboard dependencies.

Future tidy long-form result tables can be added here after confirming that they exactly preserve the original numerical results.

